# TODO

- Update packages to fix vulnerabilities.
- Publish
- Show number of credit hours total on the graph.
- Is there a way to include pre/coreqs on the course node itself? That would make reading the config file much easier; it is difficult to see if prereqs are missing at the moment.
- Add style to a node that has a prereq that is not on the graph.
- Add hover tooltip to show the prereqs of a node.
- Add ability to select track and have it filter the graph based on new track definitions.
- Configurable styles? These are hardcoded for now in layout.js.
- Evaluate whether this is still worth porting to "program-planner" repo. If not, this needs cleaned up / refactored / put under test.
